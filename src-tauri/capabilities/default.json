{
  "$schema": "https://schema.tauri.app/capabilities/2",
  "identifier": "default",
  "description": "ClarityDesk default capabilities. The page only calls the app's own commands; files and shortcuts are handled in Rust.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
